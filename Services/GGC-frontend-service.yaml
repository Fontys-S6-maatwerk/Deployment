apiVersion: v1
kind: Service
metadata:
  name: GGC-frontend
  namespace: GGC
  labels:
    app: GGC-frontend
    service: GGC-frontend
spec:
  selector:
    app: GGC-frontend-pod
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 80
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: GGC-frontend
  namespace: GGC
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: GGC-frontend-pod
      version: latest
  template:
    metadata:
      labels:
        app: GGC-frontend-pod
        version: latest
    spec:
      containers:
        - name: GGC-frontend-container
          image: ghcr.io/fontys-s6-maatwerk/ggcwebsite:main
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          ports:
            - containerPort: 80
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: GGC-frontend-autoscaler
  namespace: GGC
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: GGC-frontend
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 75
