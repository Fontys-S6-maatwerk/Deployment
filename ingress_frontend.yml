apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ggc-fe-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ggc-fe-service
            port:
              number: 80
